Flask>=2.0 # Or specific version you prefer
requests>=2.25
beautifulsoup4>=4.9
dnspython>=2.0
python-whois>=0.8 # Check latest version if desired
ipwhois>=1.2
python-dateutil>=2.8
builtwith>=1.3 # Check latest version if desired
gunicorn>=20.0 # For deployment (WSGI Server)
html.parser # This is built-in, but listing to be clear bs4 uses it
# lxml # Optional parser for bs4, install if needed/preferred